<p class="tabs">
<%- for repeat in %w{all} + Competition::REPEATS -%>
  <%= link_to repeat.titleize, puzzle_competitions_path(:repeat => (repeat unless repeat == 'all')), :class => ('checked' if repeat? repeat) %>
<%- end -%>
</p>

<%= partial @competitions %>

<%= will_paginate @competitions %>