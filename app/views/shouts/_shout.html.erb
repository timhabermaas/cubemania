<li class="shout">
  <cite><%= link_to shout.user.name, shout.user %></cite>
  <small>
    <%=dt shout.created_at %>
    <%= link_to 'Destroy', puzzle_competition_shout_path(@puzzle, @competition, shout), :method => :delete, :confirm => 'Are you sure?' if moderator? %>
  </small>
  <div class="text">
    <%=m shout.content %>
  </div>
</li>