<time dnf='<%= average.dnf? %>' record='<%= average.record? %>' date='<%=dt average.created_at %>' comment='<%=h average.comment.gsub("'", '"').gsub(/\n/, '<br>') unless average.comment.nil? %>' singles='<%= average.singles.map { |s| s.dnf? ? 'DNF' : t(s.time) }.join(', ') %>'><%= average.time %></time>