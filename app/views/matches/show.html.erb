<small>(<%= @match.puzzle.name %> <%= @match.puzzle.kind.name %>)</small>
<h1>Match between <%= link_to @match.user.name, @match.user %> and <%= link_to @match.opponent.name, @match.opponent %></h1>

<% if @match.challenged? and @match.user == current_user %>
<p class="information">
Get yourself a cup of coffee and wait for <%= link_to @match.opponent.name, @match.opponent %>!<br />
Oh, and don&#39;t worry, <strong<%=' class="dnf"' if @match.averages.first.dnf? %>><%=t @match.averages.first.time %></strong> will be fast enough to win :).
</p>
<% else %>

<% if @match.finished? %>
  <%= render 'result' %>
<% else %>
  <%= partial 'shared/timer' %>
  <%= partial 'shared/timer_js', :url => puzzle_match_times_path(params[:puzzle_id], @match) %>
<% end %>
<% end %>