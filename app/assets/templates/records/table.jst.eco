<% if @records.length == 0 and @displayChallenge: %>
  <p class="challenge">
    Be the first: <a href="<%= @timerPath %>">Get the record!</a>
  </p>
<% else: %>
  <table id="records">
    <tbody>
    </tbody>
  </table>
<% end %>