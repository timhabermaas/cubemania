// Generated by CoffeeScript 1.10.0
(function() {
  Cubemania.Presenters.RecordsPresenter = (function() {
    function RecordsPresenter(records) {
      this.records = records;
    }

    RecordsPresenter.prototype.flashMessage = function(single) {
      var messages, record;
      messages = (function() {
        var i, len, ref, results;
        ref = this.records.models;
        results = [];
        for (i = 0, len = ref.length; i < len; i++) {
          record = ref[i];
          if (record && record.containsSingle(single)) {
            results.push("You have a new " + (record.title()) + " record: <strong>" + (formatTime(record.get("time"))) + "</strong>! <a href='" + (record.getHtmlUrl(Cubemania.currentUser.get("slug"))) + "'>Share it!</a>");
          } else {
            results.push(void 0);
          }
        }
        return results;
      }).call(this);
      return _.compact(messages).join("<br />");
    };

    return RecordsPresenter;

  })();

}).call(this);
